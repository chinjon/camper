const getUserParams = require('./getUserParams');
const parseParams = require('./parseParams');

const userConfig = (params) => {

const parsedUserParams = parseParams(params);

}

module.exports = userConfig;